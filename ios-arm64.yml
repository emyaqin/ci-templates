.libretro-ios-arm64:
  stage: build-shared
  tags:
   - mac-apple-silicon
  variables:
    MAKEFILE: Makefile
    MAKEFILE_PATH: .
  before_script:
    - export NUMPROC=$((1 + $(sysctl -n hw.ncpu)/5))
  artifacts:
    paths:
    - ${CORENAME}_libretro_ios.dylib
    expire_in: 10 min
  dependencies: []

.libretro-ios-arm64-make-default:
  extends: .libretro-ios-arm64
  variables:
    platform: ios-arm64
    CC:       clang
    CXX:      clang++
  script:
    - make -C ${MAKEFILE_PATH} -f ${MAKEFILE} -j${NUMPROC} platform=ios-arm64
  after_script:
    - if [ ! -f ${CORENAME}_libretro_ios.dylib ]; then mv ${MAKEFILE_PATH}/${CORENAME}_libretro_ios.dylib ${CORENAME}_libretro_ios.dylib; fi
    # cleanup, but not for projects with a cache; basing this off corename is a workaround for not being able to ask/know if a core uses a cache
    - if [ "${CORENAME}" != "mame" -a "${CORENAME}" != "same_cdi" ] ; then mv ${CORENAME}_libretro_ios.dylib $TMPDIR ; git clean -dxf ; mv $TMPDIR/${CORENAME}_libretro_ios.dylib . ; fi
    - vtool -show "${CORENAME}_libretro_ios.dylib" || true
    - if [ x"$(vtool -show ${CORENAME}_libretro_ios.dylib | grep IOS)" = x ]; then echo wrong platform; exit 1; fi
